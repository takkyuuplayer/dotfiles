---
name: gh-comment-from-session
description: "Agent とのセッション会話を引用材料にして、GitHub の issue / PR コメントやレビュー返信の本文を作る。要約を本文に書き、根拠となる会話ログを <details> の折りたたみで全文添付し、そのままブラウザに貼れるようクリップボードへコピーするコマンドを提示する。TRIGGER when: 'この会話をコメントにして'、'いまのやりとりを PR に書いて'、'issue にコメント書いて'、'レビューに返信して'、'議論の経緯を残して'、'会話ログを貼って'、'調査結果をコメントに'、'post this discussion to GitHub' などのフレーズや、セッションで調査・判断した経緯を GitHub 上の誰かに共有しようとしているとき。ユーザーが 'コメント' と 'PR/issue/レビュー' に触れたら、明示的に会話ログの引用を頼まれていなくても、このスキルの利用を検討する。"
---

# GitHub Comment from Session

セッション中の会話を一次資料として、GitHub の issue / PR コメント・レビュー返信の本文を組み立てるスキル。

**なぜ会話ログを添付するのか**: 要約だけでは「なぜその結論になったか」が落ち、後から読む人が同じ検討をやり直すことになる。一方で会話全文を本文に貼ると長すぎて読まれない。だから **本文は要約、根拠は折りたたみ** という形をとる。

## ワークフロー

### Step 1: 何へのコメントかを確定する

投稿自体はユーザーがブラウザで行うので番号を調べる必要はないが、**種別によって書き方が変わる**ため、どれなのかは押さえる。曖昧なら聞く。

| 種別 | 書き方の違い |
| --- | --- |
| issue コメント | 課題そのものへの報告・提案。背景から書く必要がある |
| PR コメント | レビュアー向け。diff から読めることは書かず、判断の理由に絞る |
| レビューコメントへの返信 | 指摘への応答。「直した」か「直さない、理由は〜」を最初に出す |

対象の PR / issue が会話中に出てきていない場合、現在のブランチから当たりを付けて確認する:

```bash
gh pr view --json number,url,title
```

### Step 2: 会話ログを抽出する

同梱スクリプトで transcript から会話を取り出す。思考ブロック・ツール呼び出し・ツール結果・system-reminder は自動で落ちる。

```bash
python3 <skill-dir>/scripts/extract_transcript.py --quote > "$TMPDIR/session-log.md"
```

主なオプション:

- `--list` — セッション一覧（日時と ID）を表示。別セッションの内容を引くときにまず使う
- `--session <id|path>` — 対象セッションの指定（既定は最新 = 通常は今のセッション）
- `--last-n <N>` — 末尾 N 発言だけ。長いセッションの終盤だけ引きたいときに
- `--since <ISO8601>` — 指定時刻以降だけ
- `--include-sidechain` — subagent の会話も含める（既定は除外）
- `--quote` — Markdown の引用記法 `>` を付ける
- `--format json` — 発言単位の構造化出力。取捨選択を自分でやりたいとき

**今のセッションを引く場合の注意**: transcript はライブで書かれているため、末尾にはこのスキルを起動した指示そのものが載る。読み手に無関係な部分は切り落とす。

### Step 3: 引用範囲を選び、外に出せない情報を落とす

#### 3-1. 取捨選択

全部貼れば良いわけではない。折りたたみの中でも、次のものは削る:

- 試行錯誤の末に捨てられたアプローチ（結論に影響しないもの）
- ツール実行の失敗と再試行など、機械的なノイズ
- 投稿先のリポジトリと無関係な話題

逆に必ず残すもの:

- 結論の根拠になった観察・数値・エラーメッセージ
- ユーザーが方針を決めた発言（「こっちでいく」「それはやめて」など）
- 検討したが採らなかった選択肢と、採らなかった理由

削った箇所は `> _(中略: 〜についての試行錯誤)_` のように明示する。無言で削ると引用の信頼性が落ちる。

#### 3-2. ローカル環境の情報を落とす

会話ログは手元の作業をそのまま写したものなので、**読み手にとって無意味なローカル環境の痕跡が必ず混ざる**。要約を書くときは自然に避けられるが、引用は機械的にコピーするぶん残りやすい。この工程を独立して置いているのはそのため。

書き出す前に次を洗い出して、置き換えるか、その発言ごと削る:

| 出るもの | 対処 |
| --- | --- |
| 一時ディレクトリ（`/tmp/...`、`$TMPDIR` の展開結果、スクラッチパッドのパス） | 削るか `<tmp>/comment.md` のように伏せる |
| ホームディレクトリを含む絶対パス（`/Users/<name>/...`） | リポジトリ相対パスに直す |
| worktree の置き場所、ローカルブランチの試行錯誤 | 読み手に無関係なので削る |
| API キー・トークン・社内固有の URL・第三者の個人名 | 削る。伏せ字にしてもログに残る事実は変わらないので、迷ったら発言ごと落とす |

機械的な洗い出しには grep が早い:

```bash
grep -nE '/Users/|/tmp/|\$TMPDIR|/private/var/folders' "$TMPDIR/session-log.md"
```

GitHub のコメントは削除しても編集履歴とメール通知に残る。投稿前が唯一の防波堤だと考えて扱う。

### Step 4: 本文を組み立てる

次の構成で書く。

```markdown
🤖 <agent> (<model>)

<結論・依頼・報告を 1〜3 文で。読み手が最初の 1 行で「自分が何をすべきか」を判断できるように>

## 経緯 / 根拠
<なぜこの結論になったかを箇条書きで数行。diff やコミット履歴を読めば分かることは書かない>

<details>
<summary>Agent との会話ログ</summary>

<!-- extract_transcript.py の出力 -->

</details>
```

守るべき点:

- **1 行目に署名**。グローバルルールで、コメントは 1 行目に `🤖 <agent> (<model>)` を置くと決まっている（issue / PR 本文なら末尾）。モデル名はこのセッションを動かしている実際のモデル ID を書く
- **簡潔に**。diff やコミット履歴から読めることは書かない。書くのは「なぜ」と、コードからは読めない文脈だけ
- **日本語**。リポジトリの既存 issue / PR が英語なら、それに合わせる（`gh issue list` などで確認）
- レビュー返信の場合、`## 経緯 / 根拠` は不要なことが多い。「指摘のとおり修正した / この理由で修正しない」を明示し、根拠だけ折りたたみに入れる

### Step 5: 書き出してクリップボードにコピーする

本文はワーキングツリー外（`$TMPDIR` またはセッションのスクラッチパッド）に書き出す。リポジトリを汚さないため。

そのうえで、**コピー用のコマンドを 1 つ提示する**。実行はユーザーに任せる。折りたたみ入りの本文は長く、ターミナルに貼り出しても確認しづらいので、手元で見比べたい部分だけプレビューし、全文はクリップボード経由で GitHub の入力欄へ渡す。

```bash
pbcopy < /tmp/xxx/comment.md
```

macOS 以外なら `xclip -selection clipboard <` や `wl-copy <` に読み替える。

コマンドと合わせて次を伝える:

- 本文の冒頭（署名〜要約まで）のプレビュー。折りたたみの中身は行数だけ示せば足りる
- 書き出したファイルのパス。貼り付け後に直したくなったら編集して再コピーできる

## 例

**入力**: 「いまの調査を #17 にコメントしといて」

**出力する本文**:

```markdown
🤖 Claude Code (claude-opus-5)

pre-push フックが遅い原因は、新規ブランチのとき `@{u}..HEAD` が解決できず全履歴を検査していたことでした。マージベースを使う形に直せば解消します。

## 経緯 / 根拠
- upstream 未設定のブランチでは `git rev-list @{u}..HEAD` が全コミットを返す
- 手元の計測で 1,800 コミット走査 → 修正後は 3 コミット
- `--not --remotes` 案も検討したが、複数リモート環境で取りこぼすため不採用

<details>
<summary>Agent との会話ログ</summary>

> **user**
>
> push が異常に遅いんだけど
...

</details>
```
