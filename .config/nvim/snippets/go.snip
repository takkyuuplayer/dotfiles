snippet     assertEqual
	if ${1:EXPECTED} != ${2:TARGET} {
		t.Fatal(fmt.Sprintf(\`$1 should be
			%#v
			but got
			%#v\`, $1, $2))
	}
snippet     assertDeep
	if !reflect.DeepEqual(${1:EXPECTED}, ${2:TARGET}) {
		t.Fatal(fmt.Sprintf(\`$1 should be
			%#v
			but got
			%#v\`, $1, $2))
	}
