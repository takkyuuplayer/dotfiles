# Git Commit Rules

This repository overrides the global rule that forbids committing to the default branch and in the
main worktree.

- Commit directly to the base branch (`main`) in the main worktree. A dedicated branch, worktree, or
  pull request is not required.
